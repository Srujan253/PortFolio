# Project Tasks (TODO)

- [x] Remove `DarkModeToggle.jsx` component and all references
- [x] Update `App.jsx` to force `dark` class on the root element
- [x] Audit all components to ensure they look good in permanent dark mode
- [x] Fix hover issue in `Skills.jsx`: make only the button/icon respond, not the whole container
- [x] Consult Stitch MCP for a premium "Skill Section" design matching the current theme
- [x] Implement the new Skill section design
